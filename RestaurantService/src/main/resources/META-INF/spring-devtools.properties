# Spring Boot DevTools Configuration
# Include SQLite JDBC in base classloader (not restart classloader)
# This ensures the SQLite driver is accessible from the base classloader
restart.include.sqlite-jdbc=/sqlite-jdbc.*\.jar

